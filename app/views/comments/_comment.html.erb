<% unless comment.archived? %>
  <p>
    <strong><%= comment.commenter + ": " %></strong>
    <%= comment.body %>
  </p>

  <p>
    <%= link_to "Редактировать",  [comment.article, comment], class: 'btn btn-warning' %>
    <%= link_to "Удалить комментарий", [comment.article, comment], data:
      { "turbo_method": :delete, turbo_confirm: "Хотите удалить этот комментарий?" },
      :class => 'btn btn-danger'
    %>
  </p>
<% end %>